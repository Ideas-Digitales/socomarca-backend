{
  "phpunit.phpunit": "artisan test",
  "phpunit.command": "docker compose exec -t workcontainer /usr/bin/bash -c \"${php} ${phpargs} ${phpunit} ${phpunitargs}\"",
  "phpunit.debuggerConfig": "Listen for XDebug",
  "phpunit.paths": {
    "${workspaceFolder}": "/var/www/html"
  },
  "intelephense.environment.phpVersion": "8.4.11"
}
