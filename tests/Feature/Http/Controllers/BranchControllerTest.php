<?php

describe('Branches tests', function() {
    it('should show unauthorized response when not having permissions')->todo();
    it('should show own branches when having permissions to see only own branches')->todo();
    it('should show empty list when having permissions to see only own branches, but without branches')->todo();
});
